# =============================================================================
# BOVI 보드게임 채팅봇 - Python 의존성 패키지
# 로컬 및 EC2 환경 모두 지원
# =============================================================================

# Django 프레임워크
Django==4.2.7

# QR 코드 생성
qrcode==7.4.2
Pillow==10.1.0

# HTTP 클라이언트
requests==2.31.0
httpx==0.25.2

# OpenAI API
openai==0.28.0

# 환경변수 관리 (선택사항, 로컬에서도 안전)
python-decouple==3.8

# 개발용 (로컬에서 사용, 설치 실패해도 무시)
# django-debug-toolbar

# =============================================================================
# EC2 프로덕션 전용 패키지 (로컬에서는 필요 없음)
# 자동 배포 스크립트에서 별도 설치
# =============================================================================

# PostgreSQL (EC2용)
# psycopg2-binary==2.9.9

# 데이터베이스 URL 파싱 (EC2용)
# dj-database-url==2.1.0

# 프로덕션 서버 (EC2용)
# gunicorn==21.2.0

# AWS 관련 (EC2용)
# boto3==1.29.0

# 정적 파일 서빙 (EC2용)
# whitenoise==6.6.0
